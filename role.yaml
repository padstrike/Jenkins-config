jenkins:
  # authorizationStrategy:
  #   roleBased:
  #     roles:
  #       global:
  #       - entries:
  #         - user: "poramet1797@gmail.com"
  #         name: "CCOE"
  #         pattern: ".*"
  #         permissions:
  #         - "Overall/Administer"
  securityRealm:
    local:
      allowsSignup: false
      enableCaptcha: false
      users:
      - id: "admin"
        name: "IT CCoE"
        properties:
        - "consoleUrlProvider"
        - "myView"
        - preferredProvider:
            providerId: "default"
        - theme:
            theme: "noOp"
        - "timezone"
        - "experimentalFlags"
        - mailer:
            emailAddress: "poramet1797@gmail.com"
        - "apiToken"
        - samlCustomProperty:
            attributes: |-
              FAILED TO EXPORT
              org.jenkinsci.plugins.saml.user.SamlCustomProperty#attributes: No configurator found for type class org.jenkinsci.plugins.saml.user.SamlCustomProperty$Attribute
        - "loginDetailsProperty"